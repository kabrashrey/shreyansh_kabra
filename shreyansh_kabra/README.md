npm run deploy -- github deployment
npm run deploy:domain -- github deployment with custom domain
Changes for custom domain in repository:
Settings → Pages → Custom domain → enter <domain-name> → Enforce HTTPS
